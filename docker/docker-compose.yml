services:
  yahoo-finance-mcp:
    build:
      context: ../mcp-services/yahoo-finance-mcp
      dockerfile: Dockerfile
    ports:
      - "8808:8808"
    environment:
      # Optional: set PROXY_URL in a .env file next to docker-compose.yml
      # e.g.  PROXY_URL=http://proxy.example.com:8080
      PROXY_URL: ${PROXY_URL:-}
    restart: unless-stopped
